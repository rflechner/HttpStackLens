{
  "name": "httpstacklens-webui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:wasm": "go run ../build-tools",
    "build:css": "tailwindcss -i ./src/input.css -o ./wwwroot/css/output.css --minify",
    "build": "npm run build:wasm && npm run build:css",
    "dev:css": "tailwindcss -i ./src/input.css -o ./wwwroot/css/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}

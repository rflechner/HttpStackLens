# Screenshots

Drop application screenshots here, then reference them from the HTML pages.

The pages already point at these filenames (see the dashed placeholder boxes):

- `request-list.png` — live request list
- `decrypted-body.png` — decrypted HTTPS response body
- `split-panes.png` — request/response split panes
- `upstream-settings.png` — upstream-proxy settings panel
- `decrypt-toggle.png` — HTTPS decryption toggle
- `decrypted-inspect.png` — decrypted request/response inspection
- `cert-cleanup.png` — certificate cleanup summary
